// Vitest setup placeholder.
// The workspace-level `vitest.config.ts` expects this file to exist under each repo
// so that `setupFiles: ['./src/test/setup.ts']` resolves correctly when running tests.

