# Web app (optional)

Many SDR-like repos use `apps/web` (Vite/React/etc.) while Netlify builds from the monorepo root.

This README is a placeholder so the layout matches common monorepos. Add your frontend package here in a real fork; the golden template does not require a web build for Echelon validation.
